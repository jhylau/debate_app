<div class = "row">
  <ul class="thumbnails row-pad-left">
    <% @debates.each do |d| %>
      <%= render partial: 'debate_box', locals: {debate: @debage_page.debate} %>
    <% end %>
  </ul>
</div>
